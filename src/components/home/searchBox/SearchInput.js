import React from 'react';

const SearchInput = () => {
  return (
    <div id="search-input">
      <input type="text"/>
      <button type="submit">O</button>
    </div>
  );
};

export default SearchInput;
