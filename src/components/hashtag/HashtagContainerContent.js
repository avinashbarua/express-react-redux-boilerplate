import React, {PropTypes} from 'react';

const HashtagContainerContent = () => {
    return (
        <div>OKOK I am content bro</div>
    );
};

HashtagContainerContent.propTypes = {
};



export default HashtagContainerContent;
