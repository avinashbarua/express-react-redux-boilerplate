import React, {PropTypes} from 'react';

const HashtagHeader = () => {
    return (
      <div className="contentOfSkew page-header">
        <h1>#hashtags</h1>
        <p>All the trending hahtags in one place!</p>
      </div>
    );
};

HashtagHeader.propTypes = {
};



export default HashtagHeader;
